# Parameters Directory

This directory contains configuration files for the PCM Tenants Configuration Setup.

## Files

- **params.txt** - Main configuration file with all tenant and product settings

## Usage

All scripts in the `scripts/` directory will automatically read from `params/params.txt`.

The web UI also reads and writes to this file when you make configuration changes.
